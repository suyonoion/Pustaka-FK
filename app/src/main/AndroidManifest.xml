<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
    android:maxSdkVersion="28" />
    

  <application
    android:allowBackup="true"
    android:icon="@drawable/ic_launcher_fk"
    android:label="Pustaka FK - Arsip Fatwa Kehidupan"
    android:roundIcon="@drawable/ic_launcher_fk"
    android:supportsRtl="true"
    android:largeHeap="true" 
    android:theme="@style/Theme.PustakaFK">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         <activity android:name=".AboutActivity" android:theme="@style/Theme.PustakaFK" />
        <activity
    android:name=".BiografiActivity"
    android:exported="false"
    android:label=""
    android:theme="@style/Theme.PustakaFK" />
        <activity android:name=".LetnanActivity" />
          <activity android:name=".SosmedActivity" />
        <activity
    android:name=".GaleriActivity"
    android:exported="false"
    android:label="Galeri Foto"
    android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar" />
        
    </application>
</manifest>
